# DeFi OneKey Frontend

一个用于查询和对比 DeFi 地址数据的现代化 React 前端应用。

## 功能特性

- 🔍 **地址查询**: 输入钱包地址，实时查询其 DeFi 持仓数据
- 🔄 **地址对比**: 比较两个地址的 DeFi 持仓差异
- ⏱️ **实时更新**: 支持订阅地址数据变化
- 🎨 **现代化 UI**: 精美的深色主题界面

## 技术栈

- **React 18** - 最新版 React
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具
- **React Router 7** - 路由管理
- **Framer Motion** - 流畅动画
- **Axios** - HTTP 请求

## 快速开始

### 安装依赖

```bash
yarn install
```

### 开发模式

```bash
yarn dev
```

应用将运行在 http://localhost:3000

### 构建生产版本

```bash
yarn build
```

### 预览生产版本

```bash
yarn preview
```

## 项目结构

```
src/
├── api/           # API 服务层
├── components/    # React 组件
├── styles/        # CSS 样式
├── types/         # TypeScript 类型定义
├── App.tsx        # 主应用组件
└── main.tsx       # 入口文件
```

## API 接口

后端 API 默认代理到 `http://localhost:8080`，可在 `vite.config.ts` 中修改。

### 预期接口

- `GET /api/address/:address` - 查询单个地址 DeFi 数据
- `GET /api/compare?addressA=&addressB=` - 对比两个地址

## 配置

环境变量配置（创建 `.env.local` 文件）：

```env
VITE_API_URL=http://localhost:8080
```

## 开发

### 代码规范

```bash
yarn lint
```

## License

MIT

